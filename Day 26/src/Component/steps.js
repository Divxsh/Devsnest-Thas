const Steps = () => {
	return (
		<div>
			<h1
				style={{
					textAlign: "center",
					fontWeight: "500",
					marginBottom: "10px",
				}}
			>
				Steps
			</h1>
			<ul>
				<li>Create a user state using redux</li>
				<li>Update user state when input is changed</li>
				<li>
					Create a seperate component and display user data(Don't Pass it
					as Props)
				</li>
			</ul>
		</div>
	);
};

export default Steps;
